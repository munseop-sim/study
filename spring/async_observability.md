# Async And Observability

## 로그 vs 메트릭
- 로그: 시스템이 수행한 동작/흐름 기록
- 메트릭: 시스템 상태(CPU, 에러율, 지연시간 등) 수치 관찰

## 동기 vs 비동기
- 동기: 호출 후 제어권이 바로 반환되지 않음
- 비동기: 제어권을 즉시 반환하고 결과를 나중에 처리

### Spring `@Async` 주의점
- 프록시 기반이므로 동일 클래스 내부 직접 호출 시 비동기 적용이 되지 않음
- 예외는 호출자에게 자동 전파되지 않아 별도 처리 필요
